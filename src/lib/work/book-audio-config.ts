/**
 * Page-turn audio is disabled until the asset is added to /public/audio/page-turn.mp3.
 * Set to true after deploying the audio file to enable sound without code changes elsewhere.
 */
export const PAGE_TURN_AUDIO_ENABLED = false;
export const PAGE_TURN_AUDIO_SRC = "/audio/page-turn.mp3";
